# dotbot config file
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc: dot_zshrc
    ~/.gitconfig: dot_gitconfig
- create:
    - ~/.config.local
- shell:
  - [sh ~/dotfiles/script/install.sh, Installing basic sotware]